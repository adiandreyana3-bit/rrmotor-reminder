<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet untuk Firebase dan WhatsApp -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Akses kontak HP -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Notifikasi Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Alarm tepat waktu -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="false"
        android:label="RR MOTOR REMINDER"
        android:icon="@drawable/icon_rr_motor"
        android:supportsRtl="true"
        android:theme="@style/Theme.RRMotorReminder">

        <activity
            android:name=".WhatsAppRedirectActivity"
            android:exported="false" />

        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
